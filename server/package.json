{
  "name": "otterbank-server",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node --watch --env-file-if-exists=.env index.js",
    "start": "NODE_ENV=production node --env-file-if-exists=.env index.js"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.0",
    "@fastify/static": "^10.1.2",
    "fastify": "^5.12.1",
    "unleash-client": "^6.0.0"
  }
}
